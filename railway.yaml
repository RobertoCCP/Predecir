services:
  - name: web
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    port: 8080
    env:
      - key: PYTHONPATH
        value: "."
